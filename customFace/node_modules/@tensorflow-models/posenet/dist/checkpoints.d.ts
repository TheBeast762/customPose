export declare function resNet50Checkpoint(stride: number, quantBytes: number): string;
export declare function mobileNetCheckpoint(resolution: number, stride: number, multiplier: number, quantBytes: number): string;
